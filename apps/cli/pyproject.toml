[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[project]
name = "marimohub-cli"
dynamic = ["version"]
description = "Fast, cross-platform CLI for marimohub"
requires-python = ">=3.9"
license = "Apache-2.0"

[tool.maturin]
bindings = "bin"
