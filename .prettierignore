# Vendored prebuilt SDK output is committed (see .gitignore) but must not be
# reformatted by `vp fmt` — it is upstream code, not ours to restyle.
vendor/

# Autogenerated (packages/config/src/spec.ts → docs:generate); do not reformat.
docs/configuration.md

# Helm chart templates are Go-templated YAML that the formatter cannot parse.
charts/
