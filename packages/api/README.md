# @marimo-hub/api

The HTTP API: Hono routes with OpenAPI 3.1 schemas (`@hono/zod-openapi`) over the core services. Responses use the `{ success, data | error }` envelope.

Part of [marimohub](../../README.md). See [docs/api.md](../../docs/api.md) for the API reference.
