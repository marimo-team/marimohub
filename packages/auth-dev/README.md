# @marimo-hub/auth-dev

Dev-bypass authentication adapter: a fixed local user with no real identity, for running the stack locally without a provider. Never enable it for real users.

Part of [marimohub](../../README.md). See [docs/auth.md](../../docs/auth.md) for configuration.
