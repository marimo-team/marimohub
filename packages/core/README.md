# @marimo-hub/core

The domain: models, services, and the port interfaces every adapter implements. Imports no vendor SDK. Test doubles and contract suites are exported from `@marimo-hub/core/testing`.

Part of [marimohub](../../README.md). See [docs/architecture.md](../../docs/architecture.md) for how it fits together.
