# @marimo-hub/config

The composition root: reads `MARIMOHUB_*` env vars, selects an adapter per `*_BACKEND` selector, and wires the system. The only package that imports concrete adapters.

Part of [marimohub](../../README.md). See [docs/configuration.md](../../docs/configuration.md) for configuration.
