# @marimo-hub/compute-docker

Compute adapter running each kernel sandbox in a local Docker container.

Part of [marimohub](../../README.md). See [docs/compute.md](../../docs/compute.md) for configuration.
